# TEMPLATE ONLY — copy to secret.local.yaml (gitignored), fill in, and
# `kubectl apply -f secret.local.yaml`. Never commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: cipherpost-secrets
  namespace: cipherpost
type: Opaque
stringData:
  CIPHERPOST_DATABASE_URL: "postgresql+asyncpg://cipherpost:CHANGEME@postgres:5432/cipherpost"
  CIPHERPOST_DATABASE_URL_SYNC: "postgresql+psycopg2://cipherpost:CHANGEME@postgres:5432/cipherpost"
  POSTGRES_PASSWORD: "CHANGEME"
  CIPHERPOST_JWT_SECRET: "CHANGEME-long-random-64-chars"
  CIPHERPOST_ADMIN_PASSWORD: "CHANGEME-on-first-login"
  CIPHERPOST_ALERT_WEBHOOK_URL: ""
  CIPHERPOST_ALERT_SLACK_URL: ""
